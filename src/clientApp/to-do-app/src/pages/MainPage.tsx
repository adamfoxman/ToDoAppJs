import { Typography } from "@mui/material";

const MainPage = () => {
   return (
      <div>
         <Typography variant="h2">Welcome in ToDos Page</Typography>
         <Typography variant="subtitle1">Feel free to open Todo list on the left and create some new tasks!</Typography>
      </div>
   );
};

export default MainPage;
